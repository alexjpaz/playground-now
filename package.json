{
  "scripts": {
      "start": "node index.js",
      "now-deploy": "now -n alexjpaz-playground-now --public -t ${NOW_TOKEN}"
  },
  "dependencies": {
    "express": "^4.15.3",
    "now": "^6.2.4"
  }
}
